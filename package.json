{
  "name": "waveform-audio-player",
  "version": "1.0.0",
  "description": "HTML5 Audio Player",
  "author": "Bernie Sumption <bernie@berniecode.com> (http://berniesumption.com/)",
  "scripts": {
    "build": "npm-run-all build:clean build:demofile build:ts build:browserify build:theme",
    "build:clean": "rimraf tmp-build && mkdir tmp-build",
    "build:demofile": "ncp src/demo/demo.html tmp-build/demo.html && ncp src/demo/demo-audio tmp-build/demo-audio",
    "build:ts": "tsc -p src",
    "build:browserify": "browserify tmp-build/js/init.js --outfile tmp-build/waveform-player.js",
    "build:theme": "lessc --no-ie-compat src/waveform-player.less tmp-build/waveform-player.css",
    "release": "npm-run-all build release:clean release:uglify release:copyfiles",
    "release:clean": "rimraf release && mkdir release",
    "release:uglify": "uglifyjs --compress --mangle --output release/waveform-player.js tmp-build/waveform-player.js",
    "release:copyfiles": "ncp tmp-build/waveform-player.js release/waveform-player.debug.js && ncp tmp-build/waveform-player.css release/waveform-player.css && ncp tmp-build/demo.html release/demo.html && ncp tmp-build/demo-audio release/demo-audio",
    "test": "tslint -c tslint.json -p src"
  },
  "dependencies": {},
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/BernieSumption/waveform-audio-player.git"
  },
  "private": true,
  "devDependencies": {
    "browserify": "^14.4.0",
    "less": "^2.7.2",
    "ncp": "^2.0.0",
    "npm-run-all": "^4.1.1",
    "rimraf": "^2.6.2",
    "tslint": "^5.7.0",
    "typescript": "^2.5.2",
    "uglify-es": "^3.1.0"
  }
}
